<ol class="breadcrumb">
    <%if(exists){%>
        <label class="error text-danger">"Car with given  Car Name does not exists"</label>
        <%}%>
</ol>
<h1 class='display-4'>Find Purchased Car By Car Name</h1>
<form method="POST" action="/jobs/foundSearchJob">
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Car Name</th>
            </tr>
        </thead>
        <tbody>
            <td><input type="text" pattern="[A-Z][A-Za-z0-9]{1,9}" name="jobName05" title="Start with capital letter, Continue with numbers of max length 6" required></input></td>
        </tbody>
        </table>
        <input type="submit" value="submit" class="btn btn-primary">
</form>